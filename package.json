{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:libs": "lerna run build --ignore @driten/accounts-frontend --ignore @driten/developers-frontend  --ignore @driten/accounts-api",
    "dev": "lerna run dev --stream",
    "generate": "lerna run generate"
  },
  "dependencies": {
    "jose": "^4.5.0",
    "date-fns": "^2.28.0"
  },
  "devDependencies": {
    "@types/node": "16.11.19",
    "@types/date-fns": "^2.6.0",
    "lerna": "^4.0.0",
    "typescript": "4.5.5",
    "tsup": "5.11.13"
  }
}